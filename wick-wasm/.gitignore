# wasm-pack output. Regenerated on every `just wasm` / CI run; never
# committed (npm-publish workflow will pull a fresh build).
pkg/

# Local-only build dirs in case someone runs cargo / npm inside this
# crate folder.
target/
node_modules/
