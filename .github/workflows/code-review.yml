name: Code Review

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  code-review:
    name: Junie Code Review
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: JetBrains/junie-github-action@v1.3.7
        with:
          junie_api_key: ${{ secrets.JUNIE_API_KEY }}
          use_single_comment: true
          prompt: "code-review"
