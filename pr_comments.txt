author:	github-actions
association:	none
edited:	true
status:	none
--
<!-- junie-bot-comment:Code Review -->
Junie successfully finished!

Result: Junie finished task successfully 
 The PR successfully implements a Metal Split-K GEMV kernel for Q4_0, addressing previous review comments regarding Phase A dispatch and Phase B data races. The optimization provides a significant (11-13%) speedup for small-m shapes common in single-token inference.

Key observations:
- Phase A correctly uses a 1D grid to ensure proper `split_id` and `row_group` calculation in the shader.
- Phase B merge kernels now use 1 thread per threadgroup to avoid concurrent write races on the output address.
- A comprehensive benchmark script and microbenchmarks are added to verify performance.
- Compiler warnings across several modules have been resolved.

Suggestions for improvement:
- `scripts/benchmark_matrix.py`: The `LLAMA_BENCH` path is hardcoded to a machine-specific absolute path. It should be made configurable.
- `wick-cli/src/main.rs`: The `--prompt-tokens` flag generates tokens in a fixed range (100..1100), which might not be unique for large $n$ or valid for all models' vocabularies.
- Repository Hygiene: Committed local benchmark logs and CSV results should ideally be kept out of version control to reduce churn.
- Phase B merge: Using 1024 threadgroups of 1 thread each is correct but could be further optimized using `dispatch_threads` with larger threadgroups for $m \le 1024$.

Overall, the code quality is high and the performance wins are well-justified. 🚀

- `scripts/benchmark_matrix.py:12: Hardcoded machine-specific absolute path for LLAMA_BENCH binary should be made configurable.`
- `wick-cli/src/main.rs:605: Token generation for --prompt-tokens should ideally sample from the model's vocabulary and skip special tokens.`
- `llama_time.txt:1: Committed machine-specific log files and benchmark results should ideally be excluded from version control.`
--
author:	Copilot (AI)
association:	none
edited:	false
status:	commented
--
## Pull request overview

This PR focuses on improving decode-time performance for LFM2 on Metal by adding a Split‑K Q4_0 GEMV path for small‑`m`/large‑`k` shapes, while also expanding benchmarking tooling and cleaning up warnings across the codebase.

**Changes:**
- Added new Metal Split‑K GEMV compute kernels (partial + merge) and integrated them into the Metal LFM2 model dispatch path.
- Extended existing GEMV benchmark tests to optionally exercise the Split‑K variant and added a CPU GEMV microbenchmark (ignored).
- Added a benchmarking matrix script + committed benchmark output artifacts; updated `wick` CLI bench to support `--prompt-tokens`.

### Reviewed changes

Copilot reviewed 11 out of 12 changed files in this pull request and generated 12 comments.

<details>
<summary>Show a summary per file</summary>

| File | Description |
| ---- | ----------- |
| `wick/src/backend/shaders/gemv_q4_0_fast.metal` | Adds Split‑K partial and merge kernels for Q4_0 GEMV. |
| `wick/src/model/metal_lfm2.rs` | Wires Split‑K GEMV into Metal model path and adds scratch buffer + pipelines. |
| `wick/tests/gemv_q4_0_bench.rs` | Adds Split‑K bench mode and a Split‑K benchmark helper. |
| `wick-cli/src/main.rs` | Adds `--prompt-tokens` to bench for controlled prefill length. |
| `scripts/benchmark_matrix.py` | Adds automation to compare `wick` vs `llama.cpp` across prompt/gen matrix. |
| `wick/tests/bench_perf.rs` | Minor cleanup + adds ignored CPU GEMV microbenchmark. |
| `wick/src/model/metal_audio_decoder.rs` | Warning cleanup / dead-field removals and unused param rename. |
| `wick/src/model/gpu_lfm2.rs` | Warning cleanup (unused parameter/locals). |
| `benchmark_results.csv` | Committed benchmark results output. |
| `benchmark_output.log` | Committed benchmark run log output. |
| `wick_time.txt` | Committed `/usr/bin/time -l` output for `wick` run. |
| `llama_time.txt` | Committed `/usr/bin/time -l` output for `llama.cpp` run. |
</details>






---

💡 <a href="/hyeons-lab/wick/new/main?filename=.github/instructions/*.instructions.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add Copilot custom instructions</a> for smarter, more guided reviews. <a href="https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn how to get started</a>.
--
